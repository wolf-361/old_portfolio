version: "3"

services:
   frontend:
       build: .
       restart: unless-stopped
       ports:
          - 127.0.0.1:10000:80
   backend:
      build: backend
      restart: unless-stopped
      ports:
         - 127.0.0.1:10001:5000